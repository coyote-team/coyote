en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      html: '<span class="form-field-label-requirement">(required)</span>'
    error_notification:
      default_message: "Please review the problems below:"
    # Examples
    labels:
      organization:
        name_confirmation: "Please type the name of the organization to confirm deletion"
      representation:
        content_uri: "Provide a link to a description for this resource"
        ordinality: "Order"
        text: "Provide a visual description"
      resource_group:
        auto_match_host_uris: 'Host URI patterns'
    hints:
      organization:
        allow_authors_to_claim_resources: 'If you set this to "Yes", authors will be able to describe a resource without an editor assigning it to them'
        name_confirmation: "We ask you to do this just to avoid any accidental deletions"
      resource_group:
        auto_match_host_uris: 'Add patterns for resource source URIs that should automatically lump them into this resource group'
        webhook_uri: "This advanced feature allows Coyote to notify external applications when descriptions change. The URI you enter here will receive an HTTP POST request containing details about the resource that changed. <a href='https://coyote-team.github.io/coyote/api/'>See the API docs for more information.</a>"
    # include_blanks:
    #   defaults:
    #     age: 'Rather not say'
    # prompts:
    #   defaults:
    #     age: 'Select your age'

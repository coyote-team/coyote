en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      html: '<span class="form-field-label-requirement">(required)</span>'
    error_notification:
      default_message: "Please review the problems below:"
    # Examples
    labels:
      organization:
        name_confirmation: "Please type the name of the organization to confirm deletion"
      representation:
        content_uri: "Provide a link to a description for this resource"
        ordinality: "Order"
        text: "Provide a visual description"
    hints:
      organization:
        name_confirmation: "We ask you to do this just to avoid any accidental deletions"
    # include_blanks:
    #   defaults:
    #     age: 'Rather not say'
    # prompts:
    #   defaults:
    #     age: 'Select your age'

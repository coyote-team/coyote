= simple_form_for @<%= singular_table_name %>, validate: true, html: {multipart: true, class: 'form-horizontal', role: "form"}, wrapper: :horizontal_form, wrapper_mappings: { check_boxes: :horizontal_radio_and_checkboxes, radio_buttons: :horizontal_radio_and_checkboxes, file: :horizontal_file_input} do |f|
  = f.error_notification
  .form-inputs
  <%- attributes.each do |attribute| -%>
    = f.<%= attribute.reference? ? :association : :input %> :<%= attribute.name %>
  <%- end -%>
  
  <%- if options[:attachments] -%>
    = render "attachments_form", f: f
  <%- end -%>

  .form-actions
    .form-actions-inner
      = link_to <%= index_helper %>_path, class: 'btn btn-info' do
        %i.fa.fa-fw.fa-list{title: "List"}>
        <%=plural_table_name.capitalize%> 
      - if action_name == 'edit'
        = link_to @<%= singular_table_name %>, class: 'btn btn-info' do
          %i.fa.fa-fw.fa-eye{title: "Eye"}>
          View this <%= singular_table_name %>
      = f.button :submit, class: 'btn btn-success'

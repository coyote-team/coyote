= nav_menu_link('Dashboard',        organization_path(current_organization))
= nav_menu_link('Assignments',      organization_assignments_path(current_organization))     if policy(Assignment).index?
= nav_menu_link('Resources',        organization_resources_path(current_organization))       if policy(Resource).index?
= nav_menu_link('Descriptions',     organization_representations_path(current_organization)) if policy(Representation).index?
= nav_menu_link('Members',          organization_memberships_path(current_organization))     if policy(Membership).index?
= nav_menu_link('Meta',             organization_meta_path(current_organization))            if policy(::Metum).index?
= nav_menu_link('Resource Groups',  organization_resource_groups_path(current_organization)) if policy(::ResourceGroup).index?

- if current_user.staff? || organization_scope.many?
  = dropdown_for organization_scope, label: current_organization? && current_organization.title || "Select an organization", id: 'select-organization' do |organization|
    = link_to organization.title, organization_path(organization)
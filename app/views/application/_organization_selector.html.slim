- if current_user.staff? || organization_scope.many?
  = dropdown_for organization_scope, label: current_organization? && current_organization.title || "Select an organization", title: "Select an organization" do |organization|
    = link_to organization.title, organization_path(organization)

- if current_user.staff? || organization_scope.many?
  = dropdown_for organization_scope, label: current_organization? && current_organization.name || "Select an organization", title: "Select an organization" do |organization|
    = link_to organization.name, organization_path(organization)

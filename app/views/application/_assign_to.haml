- full_width ||= false
-disable_with = "<i id='disable-me' class='fa fa-spinner fa-spin fa-fw'></i>"
- assign_id = css_id + "-dropdown"
%span.dropdown
  -klass = "btn btn-info dropdown-toggle "
  %button{id: assign_id, class: klass, "aria-expanded" => "false", "aria-haspopup" => "true", "data-toggle" => "dropdown", :type => "button"}
    Assign
    %span.caret
  %ul.dropdown-menu{"aria-labelledby" => assign_id}
    - users.each do |user|
      %li
        = button_to organization_assignments_path(current_organization,assignment: { image_id: image.id, user_id: user.id }), action: :create, remote: true, format: :js, class: 'btn arrange', title: "Assign image #{image.id} to #{user}", data: { disable_with: disable_with } do
          = user

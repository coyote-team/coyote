.container
  .row
    .col-md-12
      #flash-messages
        -flash.each do |name, msg|
          %div{class: flash_class(name), role: "alert", id: "flash_#{name}"}
            %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
              %span{"aria-hidden" => "true"} ×
            %p= msg

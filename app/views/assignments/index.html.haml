.index-outer
  .index
    .index-inner
      %header
        %h1#page-title{tabindex: -1}
          = title "#{title}: #{number_with_delimiter assignments.total_count}"

      %nav.index-nav{role:"navigation"}
        = paginate assignments

      = render "table", assignments: assignments

      %nav.index-nav{role:"navigation"}
        = paginate assignments

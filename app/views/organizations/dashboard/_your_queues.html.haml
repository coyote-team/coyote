.row
  .col
    %h2#your_queues.bg-dark.text-light.p-1 Resources Assigned to You for Description

    - if current_user.assigned_resources.any?
      .card-columns
        = render partial: 'organizations/dashboard/assigned_resources_group', :collection => current_user.assigned_resources.first(10), :as => :resource
    - else
      %p There are currently no representations in the 'Ready to Review' state for this orgnization
